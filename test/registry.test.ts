import { describe, it } from 'vitest';

describe('getBiome', () => {
  it.todo('returns the bonsai biome by name');
  it.todo('returns the neon-vine biome by name');
  it.todo('throws a clear error listing available biomes for an unknown name');
});

describe('listBiomes', () => {
  it.todo('returns registered biome names sorted');
});
