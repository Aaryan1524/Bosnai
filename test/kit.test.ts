import { describe, it } from 'vitest';

describe('lerpColor', () => {
  it.todo('returns the start color at t=0 and end color at t=1');
  it.todo('interpolates midpoint colors correctly');
});

describe('colorRamp', () => {
  it.todo('samples across multiple stops in order');
});

describe('smoothPath', () => {
  it.todo('produces a valid SVG path d string through given points');
});

describe('branchSegment', () => {
  it.todo('produces a tapered path from widthStart to widthEnd');
});

describe('svgDoc', () => {
  it.todo('wraps inner markup in a namespaced svg root with given dimensions');
});
